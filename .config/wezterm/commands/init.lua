local toggle_transparency = require 'commands.toggle-transparency'
local swap_theme = require 'commands.swap-theme'

local M = {
  toggle_transparency = toggle_transparency,
  swap_theme = swap_theme,
}

return M